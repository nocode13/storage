import { auth } from './auth';
import { category } from './category';
import { product } from './product';
import { user } from './user';

export const api = {
  user,
  auth,
  category,
  product,
};
